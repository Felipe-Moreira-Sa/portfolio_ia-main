import 'package:flutter/material.dart';

class PrevisaoPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Previsão'),
      ),
      body: Center(
        child: Text('Conteúdo da Previsão'),
      ),
    );
  }
}
