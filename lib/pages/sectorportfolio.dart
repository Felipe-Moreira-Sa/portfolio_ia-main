import 'package:flutter/material.dart';

class CarteiraSetoresPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Carteira de Setores'),
      ),
      body: Center(
        child: Text('Conteúdo da Carteira de Setores'),
      ),
    );
  }
}
